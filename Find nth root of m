//QUE
Find nth root of m
You are given 2 numbers n and m, the task is to find n√m (nth root of m). If the root is not integer then return -1.

Examples :

Input: n = 3, m = 8
Output: 2
Explanation: 23 = 8
Input: n = 3, m = 9
Output: -1
Explanation: 3rd root of 9 is not integer.
//Using pow function, using binary search, using newtons method
class Solution {
  public:
    int nthRoot(int n, int m) {
        // Code here
        double root=pow(m,1.0/n);
        int r=round(root);
        if(pow(r,n)==m){
            return r;
        }
        return -1;
    }
};
